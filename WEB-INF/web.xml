<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!--
      서블릿(LoginServlet 등)은 @WebServlet 어노테이션으로 등록하지만, 필터 2개는 여기서
      명시적으로 등록/매핑한다. 이유: CorsFilter가 반드시 AuthFilter보다 먼저 실행돼야
      로그인 안 된 요청에 대한 401 응답에도 CORS 헤더가 붙어서 브라우저가 응답을 읽을 수
      있는데, @WebFilter 어노테이션끼리는 실행 순서가 스펙상 보장되지 않는다.
      web.xml의 filter-mapping은 선언한 순서대로 실행되는 게 스펙으로 보장되므로 여기서 관리한다.
    -->
    <filter>
        <filter-name>CorsFilter</filter-name>
        <filter-class>com.dmart.web.CorsFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>CorsFilter</filter-name>
        <url-pattern>/api/*</url-pattern>
    </filter-mapping>

    <filter>
        <filter-name>AuthFilter</filter-name>
        <filter-class>com.dmart.web.AuthFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/api/*</url-pattern>
    </filter-mapping>

</web-app>
