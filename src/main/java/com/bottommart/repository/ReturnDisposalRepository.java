package com.bottommart.repository;

import com.bottommart.entity.ReturnDisposal;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReturnDisposalRepository extends JpaRepository<ReturnDisposal, Long> {
}
